/** Marker inserted into home `mainHtml` where the shared Our Menu section mounts (see `scripts/sync-our-menu-global.py`). */
export const OUR_MENU_SLOT_HTML = "<!--our-menu-slot-->";

/** Start of the home “About” section; PatternStrip is rendered immediately before it. */
export const HOME_ABOUT_SECTION_MARKER = '<section class="section_home-about"';
